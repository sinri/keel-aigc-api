/**
 * 阿里云 DashScope 对外入口的共享包名空间；具体客户端见 {@code textgeneration} 与 {@code multimodalgeneration} 子包。
 * <p>
 * HTTP、请求/响应与 SSE 流处理实现位于 {@code io.github.sinri.keel.aigc.api.internal.dashscope}，不通过模块导出。
 */
@NullMarked
package io.github.sinri.keel.aigc.api.llm.dashscope;

import org.jspecify.annotations.NullMarked;
